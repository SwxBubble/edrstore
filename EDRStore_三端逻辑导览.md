# EDRStore 三端逻辑导览

## 1. 先建立整体印象

EDRStore 是一个面向外包存储的原型系统：客户端希望把数据交给云端保存，同时尽量保留去重、局部压缩和相似数据差分压缩带来的空间收益，又不把明文直接交给存储服务器。

整个系统由三个独立进程组成：

- **ClientMain（Client）**：读写用户文件，切块，提取相似特征，请求密钥，完成加密/解密，并保存客户端自己的密钥配方。
- **KeyManager（KeyServer）**：根据块的相似特征生成或复用“密钥种子”，帮助相似数据采用相互关联的加密参数。
- **ServerMain（Storage Server）**：接收客户端上传的数据，在密文或压缩后数据上做去重、相似块检测和差分压缩，最后管理文件配方、块索引及容器文件。

可以先用下面这张图理解三者的关系：

```text
                         上传时请求密钥种子
                   ┌─────────────────────────┐
                   │                         ▼
用户文件 ──► ClientMain                 KeyManager
              │  切块、特征、加密              │
              │  本地保存每块解密密钥            │
              │                         返回密钥种子
              ▼
          ServerMain
       去重、差分压缩、落盘
       保存文件配方和数据块
              │
              └──────── 下载数据 ────────► ClientMain ──► 恢复文件

注意：下载链路不经过 KeyManager。
```

系统的核心分工可以概括为：**Client 掌握明文和最终密钥，KeyServer 协调相似数据的密钥种子，Storage Server 负责数据缩减和持久化。**

---

## 2. ClientMain 做什么

ClientMain 是整个数据处理流程的发起方，也是唯一真正接触原始文件明文和最终块密钥的组件。它支持上传和下载两种操作。

### 2.1 上传逻辑

上传不是单线程依次处理，而是一条由消息队列连接的处理流水线。大致过程如下：

1. **识别文件**
   
   Client 把“输入路径 + client ID”做哈希，得到服务端识别文件所用的文件名哈希。服务端不会直接使用原始路径作为文件名。

2. **文件切块并计算块指纹**
   
   按 `config.json` 中的切块配置，把文件拆成一系列块，并为每个明文块计算 SHA-256 指纹。切块方式由 `chunking_type` 决定。

3. **提取明文相似特征**
   
   从每个块中提取用于判断相似性的特征。它们不仅用于后续的压缩决策，也会批量发给 KeyServer 请求密钥种子。

4. **向 KeyServer 请求密钥种子并完成加密**
   
   KeyServer 返回的是密钥种子，不是可以直接解密文件的最终密钥。Client 再把块内容的一部分与该种子组合并哈希，得到每块最终密钥，然后执行两阶段加密。

5. **提取加密后数据的相似特征**
   
   Client 会对加密后的块再次提取特征。后续交给 Storage Server 的相似性判断主要围绕加密数据展开，而不是把明文块交给服务端。

6. **根据运行模式决定压缩和缓存策略**
   
   Client 根据 `-m` 选择仅加密、压缩后加密或完整 EDR 流程。完整 EDR 还会维护跨版本的客户端缓存元数据，判断当前块能否利用以前版本中的相似块。

7. **批量上传并保存本地密钥配方**
   
   数据块批量发送给 Storage Server。同时，Client 按文件保存一份密钥配方，其中记录每个逻辑块对应的最终密钥。上传结束时还会发送文件总大小、块数以及缓存特征淘汰信息。

上传流水线可以简化为：

```text
文件
  → 切块/块指纹
  → 明文相似特征
  → KeyServer 生成密钥种子、Client 派生最终密钥并加密
  → 加密数据相似特征
  → 选择压缩/缓存策略
  → 批量发送 Storage Server + 本地保存密钥配方
```

### 2.2 三种上传模式

| `-m` | 含义 | 大致行为 |
|---|---|---|
| `0` | Similar-aware encryption | 块经过相似性感知的密钥生成和加密，不在客户端做本地压缩。 |
| `1` | Similar-aware encryption + local compression | 块先做本地压缩和基于种子的填充，再用块密钥加密后上传。 |
| `2` | Full EDR | 根据客户端保存的跨版本特征，选择性地压缩数据，并额外维护可供服务端做差分压缩的缓存基块信息。 |

`Full EDR` 中，一个逻辑块有时会附带一个只用于服务端缓存的额外表示。这个缓存块不代表文件中多了一个逻辑块，也不会多写一条客户端密钥配方；它是为了让后续相似块有可用的差分基块。

### 2.3 下载逻辑

下载时 Client 不再连接 KeyServer，流程相对短：

1. 使用同样的“输入路径 + client ID”规则算出文件名哈希，向 Storage Server 请求文件。
2. Storage Server 先返回文件块数和原始大小，再按文件配方顺序返回各块。
3. Client 按相同顺序读取本地密钥配方。
4. 根据块类型执行解密、去填充、解压；完整 EDR 模式下还可能用服务端返回的差分块和基块重建目标块。
5. 把恢复后的明文块依次写入 `<输入路径>-d`。
6. 最后核对恢复出的块数和总字节数是否与文件配方头一致。

因此，**只保留服务端数据而丢失 Client 本地的密钥配方，现有实现无法正常恢复文件。**

---

## 3. KeyServer / KeyManager 做什么

KeyServer 是一个独立的密钥辅助服务。它不保存用户文件，不参与数据落盘，也不参与下载。

### 3.1 它接收什么

Client 批量发送每个块的相似特征。当前流程不会把完整块内容发送给 KeyServer，但这些特征来自明文块，因此 KeyServer 能看到相似性元数据。

### 3.2 它如何生成结果

KeyServer 维护一个“特征 → 已有密钥种子”的相似性索引：

- 如果新块与索引中的已有块相似，就复用对应基块的密钥种子。
- 如果没有找到相似块，就根据全局秘密和该块的完整特征集合生成新的密钥种子，并把新特征写入索引。

这样做的目的，是让相似明文块获得相同或相关的种子，为后续相似性感知加密、填充和数据缩减提供条件。

### 3.3 它不做什么

- 不接收或保存完整用户文件。
- 不生成并保存文件级“密钥清单”。
- 不直接管理 Storage Server 中的块。
- 不参与下载和解密。

真正的每块最终密钥由 Client 在拿到种子后本地派生，随后写入 Client 自己的密钥配方。因此，KeyServer 更准确的理解是**密钥种子协调服务**，而不是传统意义上保存所有文件密钥的密钥库。

当前入口中，KeyServer 的紧凑特征索引使用 `InMemoryDatabase`：查询在内存中完成，正常析构时会序列化到 `feature_key_db`，下次启动再加载。CDFE 的详细倒排表则保存在 `SimilarPolicy` 对象内部，不会随该数据库一起持久化；因此重启后紧凑特征兜底查询仍可用，但完整 CDFE 候选能力要等新数据重新建立。

---

## 4. ServerMain 做什么

严格来说，`server_main.cc` 本身只负责启动 Storage Server：加载配置、创建索引和核心处理对象、监听 TLS 连接，并为每个客户端连接启动一个处理线程。真正的存储逻辑位于它组织起来的服务端流水线中。

### 4.1 请求入口

客户端连接后，服务端先读取登录请求，取得：

- client ID；
- 上传或下载操作类型；
- 文件名哈希。

服务端会为同一个 client ID 加锁，避免同一客户端的多个请求同时修改其配方和缓存状态。不同客户端则可以由不同连接线程处理。

### 4.2 上传处理流水线

服务端上传流程大致是：

```text
网络接收
  → 精确去重与数据分类
  → 双表示去重/选择
  → 客户端相关缓存差分压缩
  → 全局相似检测和差分压缩
  → 容器落盘、更新索引和文件配方
```

各阶段的职责是：

1. **接收和初步去重**
   
   解析 Client 发来的普通块、缓存块、文件结束信息和淘汰特征。服务端为收到的数据计算指纹，判断完全相同的数据是否已经存在，并把逻辑文件的块指纹按顺序写入文件配方。

2. **双表示处理**
   
   Full EDR 可能为一个逻辑块传来压缩表示和缓存表示。该阶段处理这类成对数据，去掉已存在的内容，并选出需要继续进入后续流程的表示。

3. **客户端相关缓存压缩**
   
   服务端为每个 client ID 维护 informed cache。若新块能在该客户端缓存中找到相似基块，就尝试保存为“基块 + 差分”的形式；客户端发来的缓存插入和淘汰信息也在这里生效。

4. **全局相似检测和差分压缩**
   
   对仍然是唯一块的数据，利用全局“特征 → 基块指纹”索引寻找相似基块。差分结果确实更小时才保存差分，否则保存完整块。

5. **落盘和更新索引**
   
   实际数据被顺序写入约 4 MiB 的 container；“块指纹 → container ID、偏移、长度、块类型、基块指纹”等地址信息写入块索引。文件配方本身只按顺序记录该文件引用了哪些块。

去重与差分压缩解决的是两类不同冗余：完全相同的块只保留一份；内容相近但不完全相同的块，尽量只保留相对某个基块的差异。

### 4.3 下载处理流水线

1. 根据文件名哈希找到文件配方；不存在则拒绝下载。
2. 按配方中的块指纹查询块地址索引。
3. 从 container 中读取完整块、差分块以及必要的基块，container 读取带有内存缓存。
4. 还原服务端存储层上的差分关系，或把 Client 需要参与恢复的差分/基块组合打包返回。
5. 按文件原始顺序批量发送，最后发送结束标记。

Storage Server 不持有 Client 的最终块密钥，因此服务端恢复的是可供 Client 继续处理的加密/压缩表示；最后的解密和明文文件重组仍由 Client 完成。

### 4.4 服务端保存的主要状态

- `Recipes/`：服务端文件配方，记录文件头和有序块指纹。
- `Containers/`：真正的数据块、差分数据和基块内容。
- `Cache/`：按 client ID 管理的 informed cache 相关数据。
- `fp → chunk address`：块指纹到容器位置及块状态的索引。
- `feature → base fp`：全局相似特征到候选基块的索引。
- 统计文件和日志：记录逻辑数据量、唯一块、差分压缩量、实际写入量等。

当前 `ServerMain` 入口创建的两个主索引同样是 `InMemoryDatabase`。它们运行时驻留内存，在正常析构时写入配置指定的索引文件，并在下次启动时加载；如果进程异常终止，尚未写出的更新可能丢失。与 KeyServer 一样，`SimilarPolicy` 内部的 CDFE 详细倒排表没有一并持久化。

---

## 5. 两种“配方”不要混淆

这是理解项目最关键的边界之一：

| 配方 | 保存位置 | 内容 | 用途 |
|---|---|---|---|
| 文件配方（file recipe） | Storage Server 的 `Recipes/` | 文件大小、块数、有序块指纹 | 找到文件由哪些服务端块组成 |
| 密钥配方（key recipe） | Client 本地的 `Recipes/` | 与逻辑块一一对应的最终密钥 | 下载后逐块解密 |

两者都以文件名哈希命名，但后缀不同，且设计上位于不同机器。Storage Server 知道“去哪里取块”，Client 知道“如何解开块”。

---

## 6. 建议的源码阅读顺序

如果后续要深入源码，建议先沿业务链路读，不必一开始钻进所有工具类：

1. `EDRStore/src/app/client_main.cc`：看上传和下载分别组装了哪些线程。
2. `EDRStore/src/client/`：按上传流水线顺序看切块、特征、密钥、加密、策略选择和发送。
3. `EDRStore/src/app/km_main.cc` 与 `EDRStore/src/key_manager/basic_km.cc`：看 KeyServer 如何根据相似特征返回种子。
4. `EDRStore/src/app/server_main.cc` 与 `EDRStore/src/server/server_opt_thd.cc`：看服务端如何接收请求并组装每个客户端的流水线。
5. 上传方向依次看 `data_recv_thd`、`dual_dedup_thd`、`cache_comp_thd`、`data_writer_thd`、`storage_core`。
6. 下载方向依次看 `data_reader_thd`、`data_decoder_thd`，再回到 Client 的 `data_retriever_thd` 和 `download_writer_thd`。
7. 最后再看 `reduction/`、`chunker/`、`crypto/`、`compression/`，理解具体算法实现。

一句话记忆：**Client 决定如何保护和恢复数据，KeyServer让相似数据的密钥生成保持可协调，ServerMain 背后的存储流水线负责发现并消除可利用的冗余。**

---

## 7. 相似性检测方法详解

### 7.1 先区分“重复”和“相似”

项目里有两种容易混淆的判断：

- **重复块检测**：对 Storage Server 收到的完整块表示（通常是加密后或压缩再加密后的字节）计算 SHA-256，完整指纹已经存在才算重复。这解决的是完全相同的数据，只需要保留一份；它不是在服务端比较明文指纹。
- **相似块检测**：比较从块内多个局部区域提取出的特征。即使整个块的 SHA-256 完全不同，只要有足够多的局部内容相同，仍可能被判断为相似，并尝试用基块加差分来保存。

因此，相似性检测只负责找“可能合适的基块”，并不证明两个块相同。Storage Server 最后还会实际运行 xdelta，只有差分结果足够小才真正采用该基块。

### 7.2 当前实际启用的是 CDFE

仓库中保留了 `FinesseUtil`：它把块等分为 12 段，用 Rabin 滑动指纹从每段选特征，再组合成 3 个 super-feature。但在当前 Client 的明文和密文特征线程中，真正被调用的是 `CDFEUtil::ExtractFeature`；`FinesseUtil` 虽然仍会被构造，却没有进入当前主处理路径。

CDFE 对一个数据块分三步处理。

#### 第一步：把块切成内容定义的子块

CDFE 在一个已经由顶层 Chunker 切好的块内部再次划分子块。这里的参数固定在代码中：

- 最小子块约 256 B；
- 目标平均子块约 512 B；
- 最大子块约 1024 B；
- 边界掩码为 `0x1ff`，理想情况下约每 512 个位置出现一次候选边界；
- 每个顶层块最多保留 96 个 CDFE 子块特征。

边界检测使用 Gear 风格的累计指纹。算法从目标位置 512 B 左右向两侧搜索，在 256～1024 B 范围内选离目标位置较近的内容边界；若没找到，就在最大位置切开。这样做比固定每 512 B 切分更能抵抗前部插入或删除造成的整体错位。

#### 第二步：为每个子块生成局部特征

每个子块内部使用 24 B 窗口滑动：

1. 为每个窗口计算 Gear 风格窗口哈希，再经过一次 64 位混合。
2. 取所有窗口哈希中的最小值作为该子块的局部特征，即一种 min-hash。
3. 如果子块不足 24 B，则对整个子块做 FNV 风格哈希后再混合。

每个局部特征不仅包含 64 位 `value`，还携带：

- `subblock_rank`：它是块内第几个子块；
- `norm_pos`：该子块中心相对整个块的位置，范围约为 0～1。

所以一个 CDFE 特征可以理解成：

```text
(局部内容摘要, 子块顺序, 子块在整个块中的相对位置)
```

内容摘要用于判断局部内容是否相同，顺序和位置则为布局判断提供信息。不过当前配置把“最少位置对齐子块数”设为 0，位置对齐结果会被统计，却暂时不会影响候选是否通过；这一点在后面的打分逻辑中还会展开。

#### 第三步：再生成 3 个紧凑特征

详细子块特征列表还会被压缩成 3 个 64 位特征：按子块序号对 3 取模分成三组，即第 0、3、6…个一组，第 1、4、7…个一组，然后分别对每组特征序列做 XXHash64。

最终每个块同时带有两套表示：

- **详细 CDFE 特征列表**：用于精细候选匹配；
- **3 个紧凑特征**：用于较便宜的索引和兜底匹配。

### 7.3 `SimilarPolicy` 如何寻找基块

查找分成“CDFE 主路径”和“3 特征兜底路径”。

#### CDFE 主路径

系统维护一个倒排索引：

```text
局部特征 value
    → [(基块标识, 基块插入顺序, 该特征在基块中的 rank, norm_pos), ...]
```

对待查询块的每个局部特征，系统取出具有相同 `value` 的所有 posting，并按基块聚合。对每个候选基块统计：

- 查询块中有多少个不同子块发生匹配；
- 基块中有多少个不同子块发生匹配；
- 匹配特征的归一化位置之差是否不超过 `0.15`；
- 查询块和基块各自的子块总数。

当前代码使用下面的近似 Jaccard 分数：

```text
intersection_proxy = min(查询侧匹配子块数, 基块侧匹配子块数)

union_proxy = 查询块子块总数 + 基块子块总数 - intersection_proxy

jaccard_proxy = intersection_proxy / union_proxy
```

当前阈值是 `jaccard_proxy >= 0.25`。`cdfe_min_matched_subblocks` 和 `cdfe_min_aligned_subblocks` 当前都设为 0，因此位置对齐数虽然被统计，但目前没有形成额外的硬门槛；实际生效的主要门槛是 0.25 的近似 Jaccard 分数。

合格候选按 Jaccard 分数从高到低排序。分数相同则优先选择更早加入索引的基块，最多返回 4 个候选。每个局部特征的 posting list 最多保留最近 64 条，防止过于常见的特征无限膨胀。

#### 3 个紧凑特征兜底路径

如果 CDFE 主路径没有找到候选，就逐一查询 3 个紧凑特征：

- 只要至少一个特征命中，当前实现就把块判为相似；
- 如果多个特征指向同一个基块，则该基块得到更多票；
- 选择得票最多的基块；
- 如果所有命中的候选都只有 1 票，则使用按特征检查顺序遇到的第一个基块。

这个兜底条件比 CDFE 主路径宽松，重点是尽量不要漏掉可尝试的候选。是否真的值得差分保存，仍由后面的实际压缩结果决定。

### 7.4 找到“相似”后还要做实际验证

Storage Server 的全局相似检测最多拿 CDFE 返回的前 4 个基块逐个运行 xdelta，选差分结果最小的一个。只有：

```text
最小差分大小 < 当前块大小 × 20%
```

才真正把当前块存成差分块；否则当前块仍按完整基块保存，并进入特征索引。这意味着特征匹配允许一定误判，因为最终有一个实际压缩收益检查兜底。

服务端的 informed cache 路径也使用相同的 20% 门槛，但目前只按 `SimilarPolicy` 选出的基块尝试一次。

### 7.5 相似性检测在系统中出现了三次

| 位置 | 检测对象 | 目的 | 判定特点 |
|---|---|---|---|
| KeyServer | 明文块的 CDFE 特征 | 决定复用哪个 seed | CDFE 主路径，失败后用 3 特征兜底；使用第一候选。 |
| Client 的 Full EDR 缓存判断 | 加密块的 CDFE `value` | 判断跨版本缓存中是否已有相似数据 | 当前版本命中数至少为 `max(4, 特征数×25%)`，这里只检查 value 是否存在，不计算 Jaccard、rank 或位置。 |
| Storage Server | 加密或压缩后表示的 CDFE 特征 | 选择差分基块 | CDFE 最多给 4 个候选，再由 xdelta 和 20% 门槛确认。 |

Client 会提取两次特征：加密前的特征发给 KeyServer；完成加密后再重新从密文提取特征，供 Client 缓存决策和 Storage Server 数据缩减使用。两次特征处在不同的数据域，不能混为同一个索引。

---

## 8. KeyServer 的 seed 分配详解

### 8.1 seed 不是随机编号，而是“相似簇的确定性标识”

从当前实现看，KeyServer 没有 seed 池、计数器或随机分配器。它的逻辑是：先通过相似性检测判断当前块属于哪个已有相似簇；属于已有簇就复用该簇 seed，否则根据当前块特征确定性地计算一个新 seed。

可以把它理解为：

```text
块特征
  → 找已有相似代表块
      ├─ 找到：返回代表块的 seed
      └─ 找不到：由当前特征计算新 seed，并把它登记成新的代表
```

### 8.2 Client 发给 KeyServer 的内容

Client 在明文块上运行 CDFE，然后按批次发送：

- 3 个 64 位紧凑特征；
- CDFE 特征数量；
- 每个 CDFE 特征的 `value`、`subblock_rank` 和 `norm_pos`。

当前请求结构没有发送完整明文块；早期“发送块指纹”的代码也已被注释。但因为这些特征直接来自明文，它们仍然会泄露块之间的相似关系。

### 8.3 已有相似块：复用 seed

KeyServer 把请求特征交给 `SimilarPolicy`：

1. 优先查详细 CDFE 倒排表，按近似 Jaccard 选择最高分候选。
2. 如果 CDFE 没有候选，再查 3 个紧凑特征索引并投票。
3. 命中后，候选记录中的 32 B 标识本身就是此前保存的 seed。
4. KeyServer 原样把这 32 B `key_seed` 返回给 Client。

相似块本身不会再次扩展 KeyServer 的特征索引。只有首次成为新代表的“非相似块”会登记特征。因此索引更像一组相似簇代表，而不是包含所有历史块。

### 8.4 没有相似块：生成并登记新 seed

若两级查询都未命中，KeyServer 拼接下面这些字节：

```text
32 B global_secret
|| 3 个 64 位紧凑特征
|| 32 位 CDFE 特征数量
|| 完整 CDFEFeature 数组
```

然后计算：

```text
new_seed = SHA-256(上述拼接结果)
```

当前原型里的 `global_secret` 不是从配置、密钥文件或随机源加载，而是在构造 `BasicKM` 时直接填成 32 个值为 `0x01` 的字节。所以新 seed 对同一组特征是确定性的；这适合原型复现实验，但不能视作生产级秘密管理。

生成新 seed 后，KeyServer 做两类登记：

- 把 3 个紧凑特征分别映射到该 seed；
- 把每个详细 CDFE 特征加入以该 seed 为基块标识的倒排 posting。

紧凑索引是单值映射，某个相同紧凑特征以后再次被新代表写入时会覆盖旧值；CDFE 索引则可为同一局部特征保留多个候选 posting。

### 8.5 Client 拿到 seed 后如何得到最终密钥

KeyServer 返回的 seed 不是最终加密密钥。Client 对每个块计算：

```text
chunk_key = SHA-256(明文块开头 32 B || key_seed)
```

随后：

- `chunk_key` 用于两阶段块加密，并写入 Client 本地 key recipe；
- `key_seed` 的 32 个字节被逐字节求和得到一个 64 位数，作为压缩后填充的伪随机种子。

所以“相似块复用 seed”并不等于“相似块一定使用完全相同的最终密钥”。最终密钥还混入了各自的明文内容；只有相关明文和同一 seed 共同作用，才形成该方案希望保留的相似性感知属性。

### 8.6 一个简化例子

假设 KeyServer 初始为空：

1. 块 A 到达，没有候选：计算 `seed_A`，把 A 的特征登记为一个新代表。
2. 块 B 到达，CDFE 与 A 的近似 Jaccard 为 0.6：B 直接复用 `seed_A`，但 B 不作为新代表写索引。
3. 块 C 到达，与 A 不相似：计算 `seed_C`，登记为第二个代表。
4. 块 D 同时与 A、C 有局部匹配：优先选择 Jaccard 更高者；若相同，则 CDFE 路径优先更早登记的代表。
5. Client 分别用各块开头 32 B 与返回 seed 派生最终 `chunk_key`，所以 A 和 B 共用 seed，不代表两者 key recipe 中保存的最终密钥必然相同。

### 8.7 阅读当前实现时要留意的原型特征

- `global_secret` 是硬编码常量，不是安全部署中的真实主秘密。
- KeyServer 能看到明文相似特征和访问批次，从隐私角度并非“什么都不知道”。
- `InMemoryDatabase` 在正常析构时会序列化紧凑特征索引，但 CDFE 倒排表没有持久化；异常终止还可能丢失本轮索引更新。
- seed 分簇受数据到达顺序影响：谁先被判为非相似，谁就成为新代表；CDFE 同分时也偏向更早代表。
- KeyServer 为不同连接启动线程并共享一个 `SimilarPolicy`。底层 `InMemoryDatabase` 有读写锁，但 `SimilarPolicy` 内部的 CDFE 容器没有显式锁，多客户端并发更新时需要进一步检查线程安全。
- 当前 `Client.user_key` 会被配置解析，但没有进入这条 seed 或最终块密钥派生主链路。
